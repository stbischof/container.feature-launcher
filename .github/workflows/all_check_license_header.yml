name: License Header check

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  call-license-header-check:
    uses: eclipse-osgi-technology/.github/.github/workflows/reuse_all_check_license_header.yml@main
    secrets:
      envGH: ${{ secrets.GITHUB_TOKEN }}
